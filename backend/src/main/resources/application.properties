spring.application.name=backend
server.servlet.context-path=/api/v1


spring.datasource.url=${DB_URL}
spring.datasource.username=${DB_USERNAME}
spring.datasource.password=${DB_PASSWORD}

jwt.secret=${JWT_SECRET}

app.base-url=${BASE_URL}

server.port=${PORT:8080}

spring.jpa.hibernate.ddl-auto=update

spring.jpa.show-sql=true

spring.jpa.properties.hibernate.format_sql=true

#server.port=8080
#
#jwt.secret=AmanSharma=ThisIsMySecretKeyToMakeSecretJWTToken
jwt.expiration=86400000

spring.servlet.multipart.max-file-size=50MB
spring.servlet.multipart.max-request-size=50MB

cloudinary.cloud-name=hsu6oxwy
cloudinary.api-key=466575471951774
cloudinary.api-secret=Msb1u5Sgpo24JnRZQONIaVia8vo

spring.ai.google.genai.api-key=${GEMINI_API_KEY}
spring.ai.google.genai.chat.options.model=gemini-3.5-flash-lite
spring.ai.google.genai.chat.temperature=0.5

spring.mail.host=${mail-host}
spring.mail.port=${mail-port}

spring.mail.username=${mail-username}
spring.mail.password=${mail-password}

spring.mail.properties.mail.smtp.auth=true
spring.mail.properties.mail.smtp.starttls.enable=true

app.frontend-url=${mail-frontend-url}

spring.mail.properties.mail.smtp.connectiontimeout=5000
spring.mail.properties.mail.smtp.timeout=5000
spring.mail.properties.mail.smtp.writetimeout=5000

brevo.api.key=${BREVO_API_KEY}